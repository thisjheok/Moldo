{
  "name": "speaking-trainer-monorepo",
  "private": true,
  "packageManager": "pnpm@11.0.9",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @repo/web",
    "build": "npm run build --workspace @repo/web",
    "lint": "npm run lint --workspace @repo/web"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "sharp",
      "unrs-resolver"
    ]
  }
}
